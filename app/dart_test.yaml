tags:
  integration:
    # Hits the real local backend (api/, docker compose up) - not run in CI,
    # which has no live backend for this job. Run locally with:
    #   flutter test --tags=integration
