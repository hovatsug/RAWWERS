services:
  web-admin:
    container_name: rawwers-web-admin
    build:
      context: .
      args:
        NEXT_PUBLIC_API_BASE_URL: ""
    restart: unless-stopped
    environment:
      NEXT_PUBLIC_API_BASE_URL: ""
    networks:
      - rawwers_edge

networks:
  rawwers_edge:
    external: true
